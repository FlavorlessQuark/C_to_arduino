# C_to_arduino

Makefile is kinda self explanatory, change variables as needed
If there's a linker error, you probably need to include the proper libraries and link the
.so or .a files associated , or compile them by hand
